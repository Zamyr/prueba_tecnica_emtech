{
  "watch": ["server"],
  "ext": "ts,js",
  "ignore": ["server/**/*.test.ts"],
  "exec": "ts-node --project tsconfig.server.json server/index.ts",
  "env": {
    "NODE_ENV": "development",
    "PORT": "3001"
  }
}
